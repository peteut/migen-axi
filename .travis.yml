os: linux
dist: xenial
language: python
python:
    - 3.12
env:
    - TOXENV=py312
install:
    - pip install -U pip
    - pip install tox
    - pip install coveralls
script:
    - tox
after_success:
    - coveralls
